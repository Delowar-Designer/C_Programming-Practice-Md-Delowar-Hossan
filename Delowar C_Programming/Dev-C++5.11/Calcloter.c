#include <stdio.h>
#include <math.h> // ?????? ????????? ??????? ????

int main() {
    double num1, num2, result;
    char operator;

    printf("?????????? ???????????\n");
    printf("????? ?????? ?????? ????: ");
    scanf("%lf", &num1);

    printf("??????? ?????? ???? (+, -, *, /, ^): ");
    scanf(" %c", &operator);

    if (operator != '^') {
        printf("???????? ?????? ?????? ????: ");
        scanf("%lf", &num2);
    }

    switch (operator) {
        case '+':
            result = num1 + num2;
            break;
        case '-':
            result = num1 - num2;
            break;
        case '*':
            result = num1 * num2;
            break;
        case '/':
            if (num2 != 0) {
                result = num1 / num2;
            } else {
                printf("??? ??? ???? ??\n");
                return 1;
            }
            break;
        case '^':
            printf("?????? ?????? ????: ");
            scanf("%lf", &num2);
            result = pow(num1, num2);
            break;
        default:
            printf("???? ???????\n");
            return 1;
    }

    printf("?????: %lf %c %lf = %lf\n", num1, operator, num2, result);

    return 0;
}

