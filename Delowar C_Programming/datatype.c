#include <stdio.h>
#include <limits.h>
int main()
{
    //first create a variable
    /*int number_1 = 15;
    printf("the value of integer is: %d",number_1);*/
    //find how many byte a integer store in your computer
    //use sizeof()
    //printf("size of int in my colmputer is: %d",sizeof(int));
    //find the range of intger
    /*printf("THE max RANGE OF IntEGER(in) IS: %d\n",INT_MAX);
    printf("THE min RANGE OF IntEGER(in) IS: %d\n",INT_MIN);
    printf("THE max RANGE OF LONG IntEGER(long in) IS: %ld\n",INT_MAX);
    printf("THE min RANGE OF LONG IntEGER(long in) IS: %ld\n",INT_MIN);*/
    
    /*int a = -25;
    int b = 25;
    printf("%d\n",a);
    printf("%d\n",b);*/
    
    //FLoAT.DOUBLE.CHAR DATA TYPE
    //FLOAT & DOUBLE ARE USE TO STORE THE REAL NO - 2,5,52,67,10,
    /*float salary = 25.55;
    double price = 25000.52;

    printf("the value of float is: %.2f\n",salary);
    printf("the value of double is: %.3lf\n",price);*/
    //finding the size
    /*printf("the size of float in my computer is: %ld\n",sizeof(float));
    printf("the size of double in my computer is: %ld\n",sizeof(double));*/

    //char datatye
    //f,h,a,b
    /*char alphabet = 'D';
    printf("the value of character is: %c",alphabet);*/
    return 0;
}